extends layout

block content
	div(class="start-text")
		p(class="start-text-paragraph") Ready to learn more javascript methods ?
		button(id="start-button" type="submit") Yes Ready
	div(id="start-container")
	div(id="display")